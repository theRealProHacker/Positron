name: Visualize the codebase
on: 
  push:
    branches: 
      main

jobs:
  visualize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: githubocto/repo-visualizer@0.8.2